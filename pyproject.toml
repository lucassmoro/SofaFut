[project]
name = "sofafut"
version = "0.1.0"
description = "Arquitetura MVC para o projeto SofaFut."
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.7",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
